<div class='cards'>
  <% @shoes.each do |shoe|%>
    <div class="card">
      <%= image_tag shoe.image_url %>
      <div class="card-infos">
        <div>
          <h2><%= shoe.model %></h2>
          <p><%= shoe.brand %></p>
        </div>
        <h2 class="card-info-pricing"><%= shoe.price %> €</h2>
      </div>
    </div>
  <% end %>
</div>
